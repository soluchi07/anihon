E2E tests use Playwright and are skipped by default.

To run:
1) Install Playwright: npm install -D @playwright/test
2) Set E2E_TESTS=1
3) Start the dev server (npm start)
4) Run: npx playwright test
